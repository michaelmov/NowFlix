import angular from 'angular';
import gridThumbnail from './grid-thumbnail.component';

export default angular.module('app.gridThumbnail', [])
    .component('gridThumbnail', gridThumbnail)
    .name;